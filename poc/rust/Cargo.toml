[package]
name = "ecl"
version = "0.1.0"
edition = "2024"
description = "A walking-skeleton interpreter for the ecl language"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "ecl"
path = "src/main.rs"

[profile.release]
strip = true
lto = "thin"

[lints.rust]
unsafe_code = "forbid"

